{
  "opencollection": "1.0.0",
  "info": {
    "name": "Tesla Fleet Energy Backup Off Grid Charging API",
    "version": "1.0.0"
  },
  "request": {
    "auth": {
      "type": "oauth2",
      "flow": "authorization_code",
      "authorizationUrl": "https://auth.tesla.com/oauth2/v3/authorize",
      "accessTokenUrl": "https://auth.tesla.com/oauth2/v3/token",
      "credentials": {
        "clientId": "{{clientId}}",
        "clientSecret": "{{clientSecret}}"
      }
    }
  },
  "items": [
    {
      "info": {
        "name": "Off Grid Charging",
        "type": "folder"
      },
      "items": [
        {
          "info": {
            "name": "Tesla Set Off-Grid Vehicle Charging Reserve",
            "type": "http"
          },
          "http": {
            "method": "POST",
            "url": "https://fleet-api.prd.na.vn.cloud.tesla.com/api/1/energy_sites/:site_id/off_grid_vehicle_charging_reserve",
            "params": [
              {
                "name": "site_id",
                "value": "",
                "type": "path",
                "description": "The energy_site_id returned by /products."
              }
            ],
            "body": {
              "type": "json",
              "data": "{}"
            }
          },
          "docs": "Sets the percent state-of-charge below which Powerwall will stop powering EV charging during a grid outage. Lets owners protect home loads while still allowing some vehicle charging when islanded."
        }
      ]
    }
  ],
  "bundled": true
}